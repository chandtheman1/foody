const checkWishlist = (parent) => {
    return parent.wishlist.length
}

const checkFavourite = (parent) => {
    return parent.wishlist.length
}

module.exports = {
    checkWishlist,
    checkFavourite
}